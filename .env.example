VITE_CHAT_GPT_API_KEY=
VITE_CHAT_GPT_MODEL=
VITE_CHAT_GPT_MAX_TOKENS=
VITE_DISCORD_TOKEN=
VITE_DISCORD_APP_ID=
VITE_DISCORD_GUILD_ID=

# Cron schedule (default: */10 * * * * = every 10 minutes)
# Examples: */5 * * * * (every 5 min), */1 * * * * (every 1 min), 0 9-20 * * * (hourly 9am-8pm)
VITE_APPLE_CRONTAB=*/10 * * * *
# Apple Store product page URL (e.g., https://www.apple.com/th/shop/buy-iphone/iphone-17-pro)
# This should be the full product page URL, not the API endpoint
VITE_APPLE_LINK=
VITE_APPLE_LINK_2=
# Postcode for store search (default: 10150 for Bangkok)
VITE_APPLE_POSTCODE=10150
# Discord webhook URL for stock notifications
VITE_APPLE_WEBHOOK=
